{
  "name": "Insight Pelican Dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "forwardPorts": [4000],
  "remoteEnv": {
    "DEVCONTAINER": "true"
  },
  "postCreateCommand": "uv venv --clear .venv && uv pip install --require-hashes -r requirements-dev.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}
