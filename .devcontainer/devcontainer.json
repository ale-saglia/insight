{
  "name": "Insight Pelican Dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "forwardPorts": [4000],
  "remoteEnv": {
    "DEVCONTAINER": "true"
  },
  "postCreateCommand": "rm -rf .venv && python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt && cd scripts/og-image-gen && npm install",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}
