{
  "name": "Insight Pelican Dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "forwardPorts": [4000],
  "remoteEnv": {
    "DEVCONTAINER": "true"
  },
  "postCreateCommand": "pip install -r requirements.txt && cd scripts/og-image-gen && npm install",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}
